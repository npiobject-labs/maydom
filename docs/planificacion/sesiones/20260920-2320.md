# Sesión 20260920-2320

1. **Qué cambió:** de las 16 notas de voz salió la planificación completa de maydom (`docs/planificacion/plan-maydom.md`: principios, menú reordenado por apartados —Hoy · Agenda · Cuerpo · Mesa · Vida · Mayordomo · Ajustes—, una sección por nota, arquitectura local-first con backend sin estado, decisión razonada de **no** hacer una app de gestión aparte, fases F0–F9 y deuda D1–D11), una bóveda Obsidian en `docs/planificacion/memoria/` (tres ADR y la primera nota de preferencias), el mock 003 (archivado luego en `docs/mocks/002-mock1.html`) y la app entera como PWA en `docs/` (build **MA-B1-20260920-004**): 17 pantallas, datos en `localStorage`, reglas del mayordomo, chat con LLM, manifest y service worker. Backend Rust con `POST /api/mayordomo` (proxy a OpenRouter, CORS), verificado por `deploy.yml`. `CLAUDE.md` y `README.md` actualizados.
2. **SHA:** `7b3bde8` (plan + mock) y `2029f65` (app + backend) en la rama `claude/loving-wozniak-nuwbab`, con PR en borrador hacia `main`. Verificado en el sandbox: prueba de humo con Chromium/Playwright sobre las 17 pantallas y ~25 acciones sin errores de JS; backend compilado y probado en local (503 sin clave, 200 con OpenRouter simulado, preflight OPTIONS). Pages y Fly solo se verifican tras fusionar a `main`.
3. **URL para probar:** tras fusionar, https://npiobject-labs.github.io/maydom/ (la app) y https://npiobject-labs.github.io/maydom/holamundo.html (backend). Hasta entonces, en el PC: `tools/arrancar.ps1` sirve `docs/` en `localhost:8081` y el backend en `8080`.
4. **Drive:** sin id en Parámetros; no aplica.
5. **Qué falta:** fusionar el PR y comprobar que `pages.yml` y `deploy.yml` quedan en verde; poner la clave `OPENROUTER_API_KEY` en Fly (D1) para que el chat del mayordomo responda (sin ella funciona con reglas); el resto de deuda (push real, foto del frigorífico, importación bancaria automática, precios en tiendas, agenda de Madrid, sincronización entre dispositivos y con Obsidian) está en §7 del plan. [SUPUESTO] el usuario tiene cuenta en OpenRouter; plan B: cambiar `OPENROUTER_URL` a otro proveedor compatible con la API de chat.
