# Sesión 20260921-0913

1. **Qué cambió:** el mayordomo pasa a consumir el LLM **por el gateway `npiobject-labs/openrouter`**, no llamando a OpenRouter (ADR-004): clave de aplicación en el secreto `LLM_API_KEY`, clave de acceso de la app en `MAYDOM_CLAVE` (cabecera `X-Clave`), `X-Operacion: maydom-<sección>` para repartir el gasto por función, reintentos solo ante 502/504 y errores traducidos con su `code`; nueva ruta `GET /api/estado`. Ocho secciones estrenan IA: menú semanal y **foto del frigorífico → stock** (cierra D3), catálogo de ejercicios, horario de suplementos, ocio en Madrid, análisis de sueño y de finanzas, interpretación de la consulta del buscador y chat. El buscador se queda en enlace + consulta interpretada, con catálogo de 30 fuentes, porque agregar precios ya lo hace `buscaproducto` (ADR-005, cierra D5 como decisión). Exportar memoria abre GitHub con el fichero listo para la bóveda (D10). Antes de todo eso, el arreglo del fallo de CI de la noche: el preflight devolvía un 204 con cuerpo y HTTP/2 en Fly lo rechaza (`curl (92)`).
2. **SHA:** `30bb366` (preflight, run [35570766575](https://github.com/npiobject-labs/maydom/actions/runs/35570766575) en **success**) y `6a804d6` (gateway e IA) en la rama `claude/loving-wozniak-nuwbab`, PR [#1](https://github.com/npiobject-labs/maydom/pull/1) en borrador. Verificado en el sandbox con Chromium: 17 pantallas y las nueve funciones con IA contra un gateway simulado, sin errores de JS; backend probado en local con `LLM_BASE_URL` apuntando al simulado (401 sin `X-Clave`, 200 con ella, preflight con `x-clave`).
3. **URL para probar:** tras fusionar, https://npiobject-labs.github.io/maydom/ (build `MA-B1-20260921-005`). Mock de la sesión anterior: `docs/mocks/002-mock1.html`.
4. **Drive:** sin id en Parámetros; no aplica.
5. **Qué falta:** dar de alta «maydom» en https://npiobject-labs.github.io/openrouter/conectar.html con presupuesto, guardar su clave como secreto `LLM_API_KEY` y una `MAYDOM_CLAVE` propia (esa se escribe en Ajustes → Clave de acceso); fusionar el PR y comprobar `pages.yml`. [SUPUESTO] el modelo por defecto del gateway (`google/gemini-2.5-flash-lite`) acepta imágenes para la foto del frigorífico; plan B: fijar la variable `LLM_MODELO` a uno multimodal. Deuda viva: D2 push real, D4 importación bancaria automática, D6 ofertas por ubicación, D7 agenda real de Madrid, D9 sincronización entre dispositivos, D11 acciones del agente.
