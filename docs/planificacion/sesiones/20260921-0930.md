# Sesión 20260921-0930

1. **Qué cambió:** se fusionó el PR [#1](https://github.com/npiobject-labs/maydom/pull/1) en squash y la app quedó publicada. Después, un agujero de verificación que el propio `CLAUDE.md` señalaba como manual: `pages.yml` daba por bueno el despliegue con `deploy-pages`, que solo prueba que el artefacto se subió. Ahora tiene un paso final que hace `curl` a la URL publicada **desde el runner** (el sandbox no alcanza `*.github.io`) y falla si la portada no trae el `build` esperado, si lo servido es el README —el síntoma de que el *Source* de Pages sigue en «Deploy from a branch»— o si alguno de los nueve recursos de la app no responde 200; la app es modular y un solo 404 la deja en blanco. El paso se probó antes de subirlo contra un servidor local en los tres casos: app correcta (pasa), README servido (falla con el mensaje y el arreglo) y build antiguo (falla). De paso, un fallo propio corregido antes de empujar: con `bash -e`, el `[ intento -lt 10 ] && sleep` de la última vuelta abortaba el paso.
2. **SHA:** `feaa373` (la fusión) y `a168158` (la verificación) en `main`. Runs en **success**: `pages.yml` [35572402152](https://github.com/npiobject-labs/maydom/actions/runs/35572402152) y `deploy.yml` [35572402157](https://github.com/npiobject-labs/maydom/actions/runs/35572402157) para `feaa373`, y `pages.yml` [35572635262](https://github.com/npiobject-labs/maydom/actions/runs/35572635262) para `a168158`, ya con la verificación nueva: «Portada OK: `<title>maydom</title>` con build MA-B1-20260921-005» y los nueve recursos en 200. No hay ningún run `pages build and deployment`, así que el *Source* está en GitHub Actions.
3. **URL para probar:** https://npiobject-labs.github.io/maydom/ · build `MA-B1-20260921-005` · bitácora en https://npiobject-labs.github.io/maydom/bitacora.html · backend en https://maydom-npiobject-labs.fly.dev/salud.
4. **Drive:** sin id en Parámetros; no aplica.
5. **Qué falta:** el mayordomo con LLM sigue esperando sus secretos (`LLM_API_KEY` del gateway y `MAYDOM_CLAVE`); hasta entonces la app funciona con el motor de reglas y `/api/mayordomo` responde 503. [SUPUESTO] el modelo por defecto del gateway acepta imágenes para la foto del frigorífico; plan B: fijar la variable `LLM_MODELO`. Deuda viva: D2 push real, D4 importación bancaria automática, D6 ofertas por ubicación, D7 agenda real de Madrid, D9 sincronización entre dispositivos, D11 acciones del agente.
