# Sesión 20260921-1419

1. **Qué cambió:** el mayordomo **quedó funcionando de verdad** (los dos secretos del gateway, el servidor corregido y una respuesta real del modelo verificada en el despliegue) y la app creció en cinco frentes: **notas dictadas y tituladas** por el LLM (título, etiquetas y tipo al guardar, con el patrón guardar-primero-analizar-después y sin pisar lo escrito a mano), **ficha del plato** con ingredientes, preparación, nutrientes y nota, más búsqueda en vivo, filtros en desplegable y **momentos múltiples** por casillas, **cruz de cerrar** en todas las ventanas emergentes, y el **buscador reconstruido** tras comprobarse que cuatro de cinco plantillas de tienda llevaban a un error. Cierra con `tools/chequeo-tiendas.mjs` y `.github/workflows/tiendas.yml`, una prueba de extremo a extremo que entra con un Chromium real en cada tienda. Guía de los secretos en `docs/planificacion/secretos-mayordomo.md` y copia en Drive.
2. **SHA:** `6750e4a` en `main` (13 commits desde el cierre anterior), build **MA-B1-20260921-013**. Runs en **success**: `pages.yml` [35598215486](https://github.com/npiobject-labs/maydom/actions/runs/35598215486) y el chequeo de tiendas [35598220846](https://github.com/npiobject-labs/maydom/actions/runs/35598220846) (16 bien, 0 con error, 12 bloquean al robot, 1 dudosa). El mayordomo respondió por el gateway con `gemini-2.5-flash-lite` a un coste de 0,0000231 $ por llamada.
3. **URL para probar:** https://npiobject-labs.github.io/maydom/ · backend en https://maydom-npiobject-labs.fly.dev/api/estado · bitácora en https://npiobject-labs.github.io/maydom/bitacora.html.
4. **Drive:** sin id en **Parámetros**, así que no aplica el paso automático; a petición del usuario se creó a mano el documento «maydom — activar el mayordomo: los dos secretos» en su carpeta de Drive. El conector solo crea ficheros: no puede escribir dentro de un documento que ya existe.
5. **Qué falta:** tres fallos propios salieron en esta sesión y conviene no repetirlos: un preflight con cuerpo rompió un despliegue, un 503 del gateway se contaba como «aún no hay clave» y dejaba el run verde con el mayordomo muerto, y una migración que solo vivía en memoria se rehacía en cada arranque. [SUPUESTO] el modelo del gateway acepta imágenes para la foto del frigorífico; plan B: fijar la variable `LLM_MODELO`. Deuda viva: D2 push real, D4 importación bancaria automática, D6 ofertas por ubicación, D7 agenda real de Madrid, D9 sincronización entre dispositivos, D11 acciones del agente, D13 dictado en navegadores sin reconocimiento de voz.
