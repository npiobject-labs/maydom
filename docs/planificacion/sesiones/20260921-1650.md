# 20260921-1650 — El dictado ya no repite palabras

- **Qué cambió**: `docs/app/voz.js`. El dictado acumulaba con `+=` los resultados finales del reconocimiento de voz, y Chrome en Android reemite resultados ya cerrados y entrega finales que amplían al anterior (`este` → `este es un` → `este es un plato`), así que se pegaban todos: «esteesteeste eseste es uneste es un plato…». Ahora los finales se guardan por índice, se detecta el reinicio de la lista tras una pausa larga y cada trozo se compacta (se descarta si ya estaba, sustituye si amplía lo anterior). El texto que ya hubiera en el campo tampoco se duplica. Build a `MA-B1-20260921-014` en `docs/index.html` y `docs/sw.js`.
- **SHA**: `a5f99ca` (rama `claude/upbeat-turing-00rvcc`, PR en borrador; falta fusionar a `main` para que Pages publique).
- **URL para probar**: https://npiobject-labs.github.io/maydom/ → Alimentación → Platos → un plato → Dictar (solo tras fusionar).
- **Resultado en Drive**: sin id de Drive en Parámetros, no se sube copia.
- **Qué falta**: fusionar el PR y confirmar en el móvil con dictado real; sigue la deuda anterior (D2, D4, D6, D7, D9, D11, D13, D14, D15).

## Verificación

Prueba de humo con Playwright sobre `docs/` servido en local, con un `SpeechRecognition` falso que reproduce el patrón del pantallazo (finales que amplían + reinicio de lista): el campo queda en «este es un plato de avena con nueces», sin errores de página. Cinco casos más en banco de pruebas: reemisión del mismo índice, lista reiniciada, parcial seguido de final, dictado normal y palabra repetida de verdad («muy muy bueno», que se conserva).
