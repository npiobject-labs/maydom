# 20260921-1710 — El dictado ya no se corta a la primera pausa

- **Qué cambió**: `docs/app/voz.js`. El reconocimiento del navegador cierra la sesión en cuanto callas un segundo y la API no expone ningún parámetro para alargarlo, así que `dictar()` lo vuelve a arrancar en `onend` mientras no pasen `silencioMs` (20 s por defecto) desde la última voz; antes de cada arranque vuelca lo dictado a la tanda cerrada, de modo que no se pierde ni se duplica. Los errores `no-speech` y `aborted` dejan de mostrar aviso mientras se sigue escuchando: son justo la pausa que ahora se ignora. Parar sigue cerrando al instante. Build a `MA-B1-20260921-015`.
- **SHA**: `8b0fcef` (rama `claude/upbeat-turing-00rvcc`, PR en borrador).
- **URL para probar**: https://npiobject-labs.github.io/maydom/ → Alimentación → Platos → un plato → Dictar (tras fusionar).
- **Resultado en Drive**: sin id de Drive en Parámetros, no se sube copia.
- **Qué falta**: fusionar y probar en el móvil cuánto silencio aguanta de verdad; ajustar `silencioMs` si 20 s se queda corto o largo. Sigue la deuda anterior (D2, D4, D6, D7, D9, D11, D13, D14, D15).

## Verificación

Banco de pruebas con un `SpeechRecognition` falso: tras `no-speech` + `onend` vuelve a arrancar sin cerrar ni avisar, y el texto de antes y de después se concatena una sola vez; pasado el margen de silencio, el siguiente `onend` sí cierra y entrega el texto. Prueba de humo con Playwright sobre `docs/` en local en «Editar plato»: dos arranques, el botón sigue en «⏹ Parar», el campo queda en «este es un plato de avena con nueces», sin toast de error; al pulsar Parar cierra sin rearrancar y conserva el texto.
