# 20260921-2235 — El extracto del banco entra también en Excel y PDF

- **Qué cambió**: nuevo `docs/app/extracto.js` (lector de extractos: CSV, `.xlsx` y PDF sin librerías; el `.xlsx` es un ZIP de XML y los flujos del PDF van en deflate, así que bastan `DecompressionStream` y `DOMParser`) y `docs/app/secciones/finanzas.js`, que pasa de «Importar CSV del banco» a «Importar extracto» y reutiliza el mismo diálogo de columnas venga el fichero de donde venga, con vista previa de la primera fila y casilla para invertir el signo. El CSV ahora salta el preámbulo (titular, IBAN) y reintenta en Windows-1252; el Excel lee la primera hoja, las cadenas compartidas y las fechas como número de serie; el PDF reconstruye las líneas por posición y cose el concepto y el importe partidos en dos líneas. Un PDF del que no sale ninguna fila ofrece, previa confirmación, que lo interprete el mayordomo (operación `finanzas-extracto`). Build a `MA-B1-20260921-016`; `extracto.js` entra en el caché del service worker y en la lista de rutas que verifica `pages.yml`.
- **SHA**: `9eba9dc` (rama `claude/stoic-pasteur-2w1k5f`, PR en borrador).
- **URL para probar**: https://npiobject-labs.github.io/maydom/#/finanzas → Importar extracto (tras fusionar).
- **Resultado en Drive**: sin id de Drive en Parámetros, no se sube copia.
- **Qué falta**: probar con extractos reales del banco (los del sandbox son sintéticos): si un PDF no da filas, el plan B del LLM necesita `LLM_API_KEY` (deuda D1). Sigue la deuda anterior (D2, D4 —ya solo el agregador PSD2—, D6, D7, D9, D11, D13, D14, D15).

## Verificación

Banco de pruebas con Chromium (Playwright) sobre `docs/` servido en local, con cuatro ficheros sintéticos: CSV en Windows-1252 con tres líneas de preámbulo, `.xlsx` con cadenas compartidas y fechas en número de serie, y el mismo PDF con el flujo comprimido y sin comprimir. Los cuatro devuelven la cabecera y las filas correctas, incluido el movimiento cuyo concepto e importe caen en líneas distintas. Prueba de humo en la UI: los tres formatos entran por el input de Finanzas, el diálogo preselecciona Fecha / Concepto / Importe (no Saldo), el toast dice «3 importados · 0 duplicados · 0 ilegibles», los conceptos se adivinan (alimentación, hosting, ingresos, vivienda, servicios web) y el modal se cierra con la cruz sin importar nada. Sin errores de JS en consola. Pages y Fly no se pueden comprobar desde la sesión: `pages.yml` solo despliega desde `main`.
