# Sesión 2026-09-22 17:35 — «Crear con IA» en la ficha del plato

1. **Qué cambió**: botón «✨ Crear con IA» debajo de «Ingredientes, preparación y nutrientes» en Nuevo y Editar plato; redacta la ficha a partir del nombre (operación `plato`) y la escribe en el formulario abierto, sin cerrarlo, para repasarla antes de guardar. Rellena también etiquetas (si están vacías) y minutos. El prompt se ha sacado a `fichaIA()`, compartido con `analizarPlato()`.
2. **SHA**: c2050b0 (rama `claude/dreamy-archimedes-gvmve8`), build `MA-B1-20260922-029`.
3. **Probar**: https://npiobject-labs.github.io/maydom/#/alimentacion?v=platos una vez mergeado a `main`. Prueba de humo: `node tools/prueba-plato-ia.mjs` (verde en la sesión).
4. **Drive**: sin id configurado, no aplica.
5. **Falta**: merge a `main` y verificación del run de `pages.yml`.
