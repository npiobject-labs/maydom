# Sesión 2026-09-22 18:30 — Cajas de texto que crecen con su contenido

1. **Qué cambió**: toda caja de texto de un formulario (`pedir()` en `nucleo.js`) crece con lo que se escribe, se dicta (`voz.js` emite `input`) o rellena la IA (`escribir()`), hasta el 60 % de la pantalla; después, barra. También se estira a mano en vertical y encoge al borrar. Vale para «Cómo se hace», la ficha del plato, el relato de la noche y el resto.
2. **SHA**: 5dac535 en `main`; build `MA-B1-20260922-031`; run de Pages 35775334566 en `success`.
3. **Probar**: https://npiobject-labs.github.io/maydom/#/ejercicio?v=catalogo · `tools/prueba-ejercicio-ia.mjs` comprueba que crece y encoge; plato y sueño siguen en verde.
4. **Drive**: sin id configurado, no aplica.
5. **Falta**: comprobar el dictado real en el móvil (el sandbox no tiene micrófono).
