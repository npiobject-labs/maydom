# Sesión 2026-09-23 21:25 — Instalar la app y aviso de versión nueva

1. **Qué cambió**: réplica en maydom de lo que hace `prueba` (leído, sin tocarlo). Nuevo `docs/app/pwa.js`: al abrir y al volver a la app compara el build publicado con el que corre y, si hay uno nuevo, enseña la banda «Hay una versión nueva de maydom · Actualizar». En Ajustes, tarjeta **Aplicación** con «Instalar la app» (cuando el navegador lo ofrece; si no, cómo hacerlo en Chrome o iPhone), el build y «Buscar actualización». Iconos PNG 192/512/maskable en el manifest. `sw.js` revalida con el servidor (`no-cache`) para que «Actualizar» no traiga módulos viejos de la caché HTTP.
2. **SHA**: 77e289b en `main`; build `MA-B1-20260923-004`; run de Pages 35909330518 en `success`.
3. **Probar**: https://npiobject-labs.github.io/maydom/#/ajustes · `node tools/prueba-pwa.mjs` (verde, 3 de 3); finanzas, buscador, cocina, plato, sueño, importar y ejercicio siguen en verde.
4. **Drive**: sin id configurado, no aplica.
5. **Falta**: verlo en el móvil. Con la app ya instalada, esta primera actualización llega aún por el service worker viejo (build 003): la banda saldrá en la siguiente publicación, no en esta. [SUPUESTO] Chrome en Android ofrecerá «Instalar la app» ahora que hay PNG de 192 y 512; plan B: menú ⋮ → «Instalar app», que la tarjeta explica. Hallazgo: Chrome puede tardar hasta ~30 s en activar el service worker nuevo aunque llame a `skipWaiting()`.
