# Sesión 2026-09-24 13:19 — Mock de accesos directos en Hoy

1. **Qué cambió**: opinión breve sobre la idea de accesos directos (a acciones, no a secciones; rejilla en vez de colocación libre; formas con nombre corto; en Hoy y no en otra pantalla; ☆ «Fijar en inicio» en cabecera en vez de un botón abajo a la derecha) y mock navegable `docs/mocks/003-accesos-directos.html`: rejilla de 4 columnas fijas con tres tamaños (1×1, 2×1, 2×2) y cinco formas (círculo, cuadrado, hexágono, pentágono, hoja), huecos permitidos, modo colocar (mantener pulsado o «✎ Colocar») con arrastrar, ✕ para quitar y huecos «+», ventana «Fijar en inicio» con destino, nombre de 12 letras, icono, color, forma, tamaño y vista previa, y sugerencias del mayordomo por uso. La app real no se toca.
2. **SHA**: 237322e en `main`; build del mock `MA-B1-20260924-001`.
3. **Probar**: https://npiobject-labs.github.io/maydom/mocks/003-accesos-directos.html · probado en local con Playwright a 390 px (tocar lleva a la acción, fijar desde la ventana de acción, mantener pulsado, intercambio al soltar encima, mover el grande dejando hueco, modo oscuro): sin errores.
4. **Drive**: sin id configurado, no aplica.
5. **Falta**: que el usuario lo pruebe en el móvil y decida si se desarrolla. [SUPUESTO] arrastrar con el dedo funciona igual que con el ratón (Pointer Events + `touch-action:none` en modo colocar); plan B: flechas de mover en la ventana de edición. Si se desarrolla: rutas `?accion=` por sección, `estado.accesos` en `maydom.v1`, atajos del manifest y ADR nuevo que complemente el ADR-003.
