# Sesión 2026-09-24 16:24 — Propuesta del especialista de suplementos

1. **Qué cambió**: análisis y crítica de «Revisar horario con LLM» (corre con `gemini-2.5-flash-lite`, el modelo por defecto del gateway, y con el prompt del mayordomo de 120 palabras; no da dosis ni conoce la jornada, la composición ni la medicación; empareja por nombre exacto y aplica los cambios sin preguntar) y propuesta en `docs/planificacion/suplementos-especialista.md`: agente `suplementos-pauta` con prompt y modelo propios (candidato Opus 5.5, elegido midiendo con un workflow de evaluación), contrato JSON por anclas, tabla local de límites EFSA y choques que manda sobre el modelo, planificador de horas en la app y «✦ Según la IA» en hora y dosis. Mock navegable `docs/mocks/004-suplementos-especialista.html`. La app real no se toca.
2. **SHA**: e1703a2 en `main`; build del mock `MA-B1-20260924-003`.
3. **Probar**: https://npiobject-labs.github.io/maydom/mocks/004-suplementos-especialista.html · probado en local con Playwright a 390 px, en claro y oscuro (preparar, pedir, entreno/descanso, óptima/sencilla, aceptar todo, aviso de límite, ficha con «✦ IA», cambiar la hora del entreno recoloca la pauta): sin errores ni scroll horizontal.
4. **Drive**: sin id configurado, no aplica.
5. **Falta**: que el usuario decida. Si sigue adelante: ADR-008, evaluación de modelos por workflow antes de fijar el modelo, y fase 1 de desarrollo. [SUPUESTO] identificador de Opus 5.5 en OpenRouter, que la pauta quepa en los 120 s del gateway y que este deje pasar `reasoning` y `provider`; los planes B están en el documento.
