# Sesión 2026-09-24 17:38 — Pauta de suplementos en Markdown, con identificación por fotos

1. **Qué cambió**: el procedimiento de la pauta externa gana un paso previo: el modelo identifica cada suplemento a partir de fotos y textos (uno por suplemento), genera `suplementos-entrada.md` y se para a que el usuario la revise; con «sigue» hace `pauta-suplementos.md`, que es el fichero que se importará en maydom. Formatos en Markdown (tablas y cabecera `formato:` con versión) en vez de Excel: `perfil.md`, `formato-entrada.md` (con Origen, Fuente etiqueta/conocida/falta y Confianza), `formato-salida.md` (Resumen, Jornada, Suplementos, Pauta, Avisos, Totales, Preguntas) y `prompt-especialista.md` reescrito (prompt 1 con pausa, 2 segunda opinión, 3 corrección). Se retiran los `.xlsx`. `suplementos-pauta-externa.md` reescrito con el porqué de Markdown y los siete problemas del procedimiento nuevo resueltos. Se comprobó que las plantillas se leen como tablas (celdas por fila e ID coherentes).
2. **SHA**: 504c81d en `main` (run de Pages 36021594121 en verde).
3. **Probar**: rellenar `perfil.md`, fotografiar los botes con su tabla de composición y pegar el prompt 1 en un chat de claude.ai con Fable 5.1 (u Opus 5.5) y razonamiento extendido.
4. **Drive**: sin id configurado, no aplica.
5. **Falta**: que el usuario genere la pauta y la pase en una sesión **sin subirla al repositorio** (datos de salud en un repo público); después, el importador de Markdown y la vista de la toma diaria. [SUPUESTO] el chat entrega `.md` descargables (plan B: bloque de código) y el plan incluye Fable 5.1 (plan B: Opus 5.5).
