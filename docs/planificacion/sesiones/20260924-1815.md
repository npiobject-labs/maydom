# Sesión 2026-09-24 18:15 — Skill «suplementos» y proceso recurrente

1. **Qué cambió**: skill de claude.ai `docs/planificacion/suplementos/skill/suplementos/` (SKILL.md + formatos y perfil como referencias), validado y empaquetado en `suplementos.zip` (y `.skill`, mismo zip). Tres modos: **añadir** un suplemento desde enlace, foto, pedido o texto a la tabla de Drive sin duplicar ni renumerar ID; **pauta**; **revisar** (segunda opinión). Documentado el proceso recurrente (skill + Drive o Claude Docs + Claude en Chrome; datos desde los enlaces de «Mis pedidos») en la §10 de `suplementos-pauta-externa.md`, las preferencias del día en `memoria/preferencias/2026-09-24.md`, una línea en el plan y la regla en `CLAUDE.md` de que la tabla, la pauta y el perfil reales nunca entran en el repositorio.
2. **SHA**: 5324aaa en `main` (run de Pages 36026158379 en verde).
3. **Probar**: subir `docs/planificacion/suplementos/skill/suplementos.zip` en claude.ai → Ajustes → Capacidades → Skills.
4. **Drive**: sin id de proyecto configurado. La carpeta privada de suplementos del usuario es suya y no es copia del repo.
5. **Falta (próxima sesión, con el usuario)**: primera inclusión en Drive: subir el skill, crear la carpeta con `perfil.md` relleno y `suplementos-entrada.md` vacío, activar el conector, «añade este suplemento» con un enlace de «Mis pedidos» y comprobar la fila en Drive. [SUPUESTO] el conector de Drive de claude.ai escribe (plan B: reemplazo a mano) y Claude en Chrome está en su plan (plan B: enlaces y capturas).
