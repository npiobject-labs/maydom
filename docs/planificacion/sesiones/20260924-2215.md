# Sesión 2026-09-24 22:15 — Notas por tipo: tareas, compras e ideas

1. **Qué cambió**: mock `docs/mocks/005-notas-por-tipo.html` (build 004), aprobado con sus cuatro mejoras, y después desarrollo (build 005). Notas pasa a tener tres pestañas, Tareas, Compras e Ideas, y un solo diálogo, `abrirNota()`, que clasifica por reglas (`docs/app/interpretar-nota.js`) y con el mayordomo en la misma llamada `nota`. Las tareas llevan fecha tope obligatoria leída del dictado y salen en Hoy y en el Calendario sin sumar carga ni avisar. Las compras se parten en líneas con cantidad y etiqueta ampliable (alimentación, droguería, farmacia, otras…) dentro de la lista de Compra, sin duplicar. Una nota que mezcla se ofrece guardar por separado, y las notas anteriores pasan a ideas. Nuevos: ADR-008, bloque en las preferencias del 24-sep, F17 en el plan, regla en `CLAUDE.md` y prueba de humo `tools/prueba-notas.mjs` (45 comprobaciones). Se ajustó `prueba-accesos.mjs`, porque la ☆ nombra ahora la pestaña.
2. **SHA**: a021f43 en `main` (fusión de la PR https://github.com/npiobject-labs/maydom/pull/21; implementación en f541c55). Run de Pages 36053824754 en verde, incluido «Verificar que Pages sirve la app».
3. **Probar**: https://npiobject-labs.github.io/maydom/#/notas (build MA-B1-20260924-005). Si el móvil tiene abierta una versión anterior, sale la banda «Hay una versión nueva · Actualizar».
4. **Drive**: sin id configurado; no aplica.
5. **Falta**: probar el dictado real en el móvil: las reglas y la llamada `nota` se han probado con el mayordomo simulado, no con el gateway. [SUPUESTO] el modelo del gateway respeta el JSON ampliado de `nota` (clase, tope, líneas, partes); plan B: las reglas rellenan igualmente y lo que falte se corrige en el diálogo.
