# Sesión 2026-09-26 13:25 — Contar el ejercicio hablando

1. **Qué cambió**: Ejercicio gana «🎤 Contar el ejercicio», con el mismo patrón que «Contar la noche» (build MA-B1-20260926-001, F19).
   - **Relato arriba y «✨ Interpretar» justo debajo** (también salta solo al terminar el dictado). El mayordomo (operación nueva `ejercicio-relato`) lo traduce a una lista de ejercicios, uno por línea: «Dominadas: 8, 6, 5 · 10 kg · descanso 1 min 30 s», con `N × 45 s` para isométricos y lo que va por tiempo. Debajo, el **volumen**: ejercicios, series, repeticiones, tiempo, descanso entre series y kg movidos, que se recalcula al corregir la lista a mano.
   - **Se guarda** en `sesionesEjercicio[]` (`maydom.v1`) con `origen: 'relato'`, el relato original y cada ejercicio con sus series (`{r}` repeticiones o `{s}` segundos), `kg` y `descanso`, enlazado al catálogo si se llama igual. Lo corregido a mano al editar queda en `manuales`.
   - **Seguimiento**: tarjeta «Últimos 7 días» con el volumen sumado (también el de las tablas hechas) y las sesiones contadas en el historial; el porcentaje de series planificadas solo cuenta las de tabla.
   - **Sin LLM**: lector por reglas en el módulo nuevo `docs/app/interpretar-ejercicio.js` («N series de R», «4x12», números en letra, segundos, kilos, descanso dicho aparte). `pedir()` gana el gancho `alAbrir`.
   - **Pruebas**: `tools/prueba-ejercicio-relato.mjs` (37 comprobaciones) en verde; siguen en verde las de ejercicio con IA, relato del sueño, accesos, pestañas de Finanzas y notas.
2. **SHA**: en la rama `claude/bold-newton-gq4pr4`, PR pendiente de fusionar. Pages solo publica desde `main`.
3. **Probar**: https://npiobject-labs.github.io/maydom/#/ejercicio (build MA-B1-20260926-001), una vez fusionada la PR y con `pages.yml` en verde.
4. **Drive**: sin id configurado; no aplica.
5. **Falta**:
   - Fusionar la PR y confirmar `pages.yml` en verde.
   - Probar `ejercicio-relato` con el gateway real: se ha probado con el mayordomo simulado. [SUPUESTO] El modelo devuelve `series` y `segundos` como listas por serie. Plan B: `desdeLLM()` acepta también `series` como número con `reps` o `seg`, y si no sale nada queda la lista de las reglas.
   - [SUPUESTO] «la base de datos del proyecto» es el estado local `maydom.v1` (ADR-002), que viaja con Exportar. Plan B: si se quiere en un servidor, es un ADR nuevo que revisa ADR-001/002.
   - [SUPUESTO] El peso es el mismo en todas las series de un ejercicio. Plan B: pirámides de peso como dos líneas del mismo ejercicio.
