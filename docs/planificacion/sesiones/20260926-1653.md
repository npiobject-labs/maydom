# Sesión 2026-09-26 16:53 — Modelo en Ajustes e informes con varios roles (ADR-010)

1. **Qué cambió**: maydom tiene lo mismo que `prueba` en Ajustes. «Modelo e informes» elige un modelo del catálogo del gateway (`GET /api/modelos`), que vale para lo que redacta: chat, consejos e informes. Hay nueve roles de serie y roles propios. En Notas → Ideas, «🔎 Analizar» saca un informe con 1 a 8 roles más una síntesis, en la nueva pestaña Informes, con progreso, reintentar lo que falló, editar, copiar, .md, PDF, compartir, regenerar y el coste real leído del gateway (`GET /api/uso`). Backend: `modelo` y `modo: "informe"` en `/api/mayordomo`. Decisión en ADR-010.
2. **SHA**: aaf8c92 en la rama `claude/cool-mccarthy-bjpsxj`, en https://github.com/npiobject-labs/maydom/pull/29. **Sin fusionar**: fusionar sin revisión no estaba permitido en esta sesión, así que no hay run de `pages.yml` ni de `deploy.yml` para este cambio.
3. **Probar** (tras fusionar y con los dos runs en verde): https://npiobject-labs.github.io/maydom/#/ajustes (modelo y roles) y https://npiobject-labs.github.io/maydom/#/notas?v=ideas → 🔎 Analizar (build MA-B1-20260926-004).
4. **Drive**: sin id configurado; no aplica.
5. **Falta**: revisar y fusionar el PR 29 y confirmar que salen en verde `pages.yml` y `deploy.yml`, incluido su paso nuevo «Verificar modelos e informes», que comprueba el catálogo real, una llamada en modo informe y su coste. En local está probado con `tools/prueba-informes.mjs` (55 comprobaciones contra el backend real y un gateway simulado) y con las pruebas de humo anteriores. Supuestos, con su plan B en ADR-010: los cinco recomendados siguen en el catálogo, y basta con que un informe a medias se reintente al volver en lugar de seguir con la app cerrada.
